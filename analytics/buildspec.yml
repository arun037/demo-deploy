version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - cd analytics/frontend
      - npm ci

  build:
    commands:
      - npm run build

  post_build:
    commands:
      - echo "Frontend build completed"

artifacts:
  files:
    - '**/*'
